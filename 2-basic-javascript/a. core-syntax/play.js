var yourName = 'nasution';
var yourAge = 12;
var hasHobbies = false;

function summarizeUser(userName, userAge, userHobbies) {
  return `User Name is ${userName} and User Age is ${userAge} and userHobbies is ${userHobbies}`;
}

console.log(summarizeUser(yourName, yourAge, hasHobbies));
console.log(summarizeUser(yourName, yourAge, hasHobbies));
